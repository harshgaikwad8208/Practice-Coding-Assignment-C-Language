#include<stdio.h>
#include<string.h>

void main()
{
    int len;
    char str[] = {"Harshal"};
    
    len = strlen(str);
    printf("Size of Array is : %d",len);
}

// Description: Returns the length of the input string.n